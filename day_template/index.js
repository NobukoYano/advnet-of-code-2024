/**
 * day X - first
 * @param {Array<string>} inputs
 * @return {number}
 */
const calculate = (input) => {

    return -1;
};

const fs = require("fs");
const file = fs.readFileSync("./dayX/input.txt").toString('utf-8');
const input = file.split("\n")

console.log(calculate(input));
